/** Tailwind CSS v4 is configured in CSS, not here — see src/app/globals.css.
 *  This file exists only to register the PostCSS plugin. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
